<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Student</title>
  </head>
  <body>
    <form action="/student/edit/<%= dataStudent.id%>" method="post">
      <label>First Name</label><br>
      <input type="text" name="first_name" value="<%= dataStudent.first_name%>" required><br>
      <label>Last Name</label><br>
      <input type="text" name="last_name" value="<%= dataStudent.last_name%>" required><br>
      <label>Email</label><br>
      <input type="text" name="email" value="<%= dataStudent.email%>" required><br>
      <select name="gender">
        <option value="male" <% (dataStudent.gender === "male") ? 'selected' : ''%> >Male</option>
        <option value="female" <% (dataStudent.gender === "female") ? 'selected' : '' %> >Female</option>
      </select><br>
      <input type="submit" value="Submit">
    </form>
  </body>
</html>
